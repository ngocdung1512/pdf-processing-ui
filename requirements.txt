# PDF processing UI - Python dependencies
# Install (inside conversion_env):
#
# 1) GPU NVIDIA (install PyTorch with CUDA first, then the rest):
#    pip uninstall torch torchvision torchaudio -y
#    RTX 50 series (5060/5070, sm_120): use nightly build:
#      pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
#    Other NVIDIA (e.g. RTX 30/40): stable CUDA 12.4:
#      pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#    pip install -r requirements.txt
#
# 2) CPU only: pip install -r requirements.txt
#
torch
torchvision
torchaudio
numpy

transformers>=4.45.0,<5.0.0
accelerate>=1.0.0
huggingface_hub>=0.34.0,<1.0

# OCR: basic (ocr_basic.py) + advanced (scripts)
paddleocr
vietocr==0.3.13
# Pin VietOCR-dependent packages to avoid resolver backtracking/conflicts.
albumentations==1.4.2
einops==0.2.0
gdown==4.4.0
imgaug==0.4.0
prefetch-generator==1.0.1
scikit-image>=0.21.0
qwen-vl-utils
markdown
htmldocx

python-docx
PyMuPDF>=1.24.0
pdf2image
opencv-python>=4.10.0
Pillow==10.2.0
ultralytics>=8.3.0
pdf2docx

# Backend API
fastapi
uvicorn[standard]
python-multipart

# DocLayout-YOLO: not in repo. For OCR nâng cao (scan), clone into project root then: pip install -e ./DocLayout-YOLO
